<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.sdei.sdeiarchitecture.R"/>
        <import type="java.util.ArrayList"/>

        <variable name="clickListener"
                  type="android.view.View.OnClickListener"/>

        <variable
                name="items"
                type="com.sdei.sdeiarchitecture.utils.common.recyclerViewBase.RecyclerBindingList" />

        <variable name="callback" type="com.sdei.sdeiarchitecture.utils.common.recyclerViewBase.RecyclerCallback"/>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="@dimen/dimen_20">

        <Button
                android:id="@+id/back_btn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/back"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                android:onClick="@{clickListener::onClick}"/>

        <androidx.recyclerview.widget.RecyclerView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/back_btn"
                app:recyclerViewAdapter="@{items}"
                app:layout="@{R.layout.row_list_item}"
                app:onItemClickListener="@{callback}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>